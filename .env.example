# Add credentials locally or in the deployment environment. Never commit a real key.
LLM_API_KEY=
LLM_MODEL=
LLM_BASE_URL=https://api.openai.com/v1
LLM_MAX_CALLS_PER_HOUR=60
# Optional access password for a shared deployment.
APP_ACCESS_PASSWORD=
# Optional absolute path; default is data/application_train.csv.
DATA_PATH=
